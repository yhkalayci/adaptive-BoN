# Alignment simplification results

The manuscript result is `final_confirmation/`, variant
`local_exp_open5_conf06`.

- `development_screen/` and `development_refinement/` are selection-only
  ablations.
- `final_confirmation/` is the untouched ten-split comparison with the former
  training-shrunk reference, including utility, matched-cost win rate, and
  target quality.

The selected policy is reproduced by
`codex_results/code/alignment_scripts/run_alignment_simple_final.sh`.
