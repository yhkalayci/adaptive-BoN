# Interrupted experiment — not a training-free result

The user subsequently requested that the method never require a training phase.
The learned-predictor process was therefore interrupted (SIGINT, exit 130).
No artifacts were deleted. The CSVs cover the first two completed generators;
additional saved Llama-3.2 split arrays are partial and must not be represented
as a completed five-generator comparison. Oracle rows use full test-pool
information and are diagnostics only, never eligible deployable methods.

This experiment is excluded from the training-free result. Its source and
partial outputs are preserved for reproducibility, not offered as satisfying
the revised requirement.
